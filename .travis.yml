language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t phamthaison/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true phamthaison/docker-react npm run test